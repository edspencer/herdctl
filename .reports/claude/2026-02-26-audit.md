# CLAUDE.md Audit Report
Generated: 2026-02-26
Measured against: canon/claude-md-gold-standard.md
Process: canon/autoclaude.md

## Summary

| Dimension | Status | Issues |
|-----------|--------|--------|
| Inventory | ⚠️ | 1 unlisted file |
| Size | ⚠️ | 2 violations |
| Style | ⚠️ | 10 descriptive lines |
| Content | ⚠️ | 3 duplication issues |
| Staleness | ⚠️ | 2 stale references |
| Coverage | ⚠️ | 1 gap |

**Total issues: 19**

---

## Inventory

### Missing Files
None — all 7 gold standard entries have corresponding files on disk.

### Unlisted Files
- `agents/engineer/CLAUDE.md` (67 lines) — exists but NOT in the gold standard inventory. This file is outside the `packages/` tree and serves a different purpose (agent persona configuration). Should be added to the gold standard inventory with its own justification, or evaluated for removal.

### Inventory Match
⚠️ 1 unlisted file found. All 7 gold standard entries are present.

---

## Size Compliance

### Violations
- `agents/engineer/CLAUDE.md` — 67 lines (no explicit target in inventory; exceeds both module-level 15–30 and package-level 30–60 ranges)
- `.claude/rules/diagrams.md` — 76 lines (target: ~30, flag threshold: 50). The bulk is a color palette table. Consider extracting the palette to a standalone reference file and having the rule point to it.

### Borderline
- `packages/web/CLAUDE.md` — 61 lines (target: 30–60, 1 line over). Cosmetic, not actionable.

### Within Range
| File | Lines | Target | Status |
|------|-------|--------|--------|
| `CLAUDE.md` (root) | 102 | 80–150 | ✅ |
| `packages/core/CLAUDE.md` | 44 | 30–60 | ✅ |
| `packages/cli/CLAUDE.md` | 41 | 30–60 | ✅ |
| `packages/chat/CLAUDE.md` | 46 | 30–60 | ✅ |
| `packages/discord/CLAUDE.md` | 40 | 30–60 | ✅ |
| `packages/slack/CLAUDE.md` | 39 | 30–60 | ✅ |

### Rules Files
| File | Lines | Status |
|------|-------|--------|
| `.claude/rules/changesets.md` | 34 | ✅ |
| `.claude/rules/claude-md-standard.md` | 10 | ✅ |
| `.claude/rules/context-management.md` | 31 | ✅ |
| `.claude/rules/diagrams.md` | 76 | ⚠️ Over 50 |
| `.claude/rules/docker.md` | 16 | ✅ |
| `.claude/rules/git-workflow.md` | 11 | ✅ |
| `.claude/rules/git-worktrees.md` | 28 | ✅ |
| `.claude/rules/staging.md` | 3 | ✅ |
| `.claude/rules/web-design-system.md` | 11 | ✅ |

Total rules files: 9 (✅ under 10-file threshold)

---

## Per-File Findings

### CLAUDE.md (root)

**Style:**
- Line 7: "We are a live project. Breaking changes require a major version bump."
  → Suggested: "Follow Semver. Breaking changes require a major version bump."
- Line 99: "Documentation lives in `docs/` and deploys to herdctl.dev."
  → Suggested: "Keep documentation in `docs/`. It deploys to herdctl.dev automatically."

Style summary: 2 descriptive lines found.

**Content:** ✅ All required sections present (project overview, repo structure, dev commands, code conventions, quality gates). No package-specific content inappropriately included.

**Staleness:**
- Line 61: "Coverage thresholds: 85% lines/functions/statements, 65% branches" — **STALE**. Actual thresholds across packages are 74–75% lines/functions/statements, 65–70% branches. No package uses 85%.
  → Fix: Update to actual thresholds or say "Coverage thresholds vary per package — see each package's `vitest.config.ts`."
- Line 82: "Architecture documentation (14 pages)" — **STALE**. Directory now contains 15 .md files (`session-discovery.md` was added).
  → Fix: Update count to 15, or remove the count to avoid future staleness.

---

### packages/core/CLAUDE.md

**Style:**
- Line 26: "All public API surfaces are re-exported through `src/index.ts`."
  → Suggested: "Re-export all public API surfaces through `src/index.ts`."

Style summary: 1 descriptive line found.

**Content:**
- Line 21: Logging convention ("Use `createLogger` — never raw `console.*`") partially duplicates root CLAUDE.md lines 64–71. The core-specific detail (relative imports within core) is valuable, but the general convention is inherited.
  → Fix: Shorten to focus only on the core-specific relative import pattern.

**Staleness:** ✅ All 17 references verified. Coverage thresholds match `vitest.config.ts`.

---

### packages/cli/CLAUDE.md

**Style:**
- Line 20: "This is a thin client. All orchestration, config parsing, scheduling, and state management belong in `@herdctl/core`. Command files here should only:"
  → Suggested: "Treat this as a thin client. Delegate all orchestration, config parsing, scheduling, and state management to `@herdctl/core`. Command files must only:"
- Line 26: "If you find yourself writing business logic, it belongs in `@herdctl/core` instead."
  → Suggested: "Move any business logic to `@herdctl/core`."
- Line 41: "Tests live in `src/commands/__tests__/` adjacent to the command files. A top-level smoke test (`src/__tests__/smoke.test.ts`) validates the built binary. Tests use Vitest and mock `@herdctl/core` dependencies."
  → Suggested: "Place tests in `src/commands/__tests__/` adjacent to command files. Use the smoke test at `src/__tests__/smoke.test.ts` to validate the built binary. Mock `@herdctl/core` dependencies in tests."

Style summary: 3 descriptive lines found.

**Content:** ✅ No duplication with root. Package-specific thin-client principle is appropriate.

**Staleness:** ✅ All 7 references verified.

---

### packages/web/CLAUDE.md

**Style:**
- Line 14: "Dark mode: Handled entirely via CSS custom properties..."
  → Suggested: "Dark mode: Use CSS custom properties on `:root` / `.dark` for dark mode. Never use Tailwind's `dark:` prefix in component classes."

Style summary: 1 descriptive line found.

**Content:**
- Line 48: "Test location: `__tests__/` directories adjacent to source" duplicates root CLAUDE.md line 59 ("Tests live in `__tests__/` directories adjacent to source").
  → Fix: Remove this line from the package file — it's inherited from root.

**Staleness:** ✅ All 21 references verified.

---

### packages/chat/CLAUDE.md

**Style:**
- Line 3: "All platform-agnostic chat logic lives here" — descriptive overview, borderline.
  → Minor: could tighten but acceptable in overview paragraph.
- Line 35: "components accept a `SessionManagerLogger` / `ChatConnectorLogger` interface rather than importing a logger directly."
  → Suggested: "Pass a `SessionManagerLogger` / `ChatConnectorLogger` interface to components. Never import a logger directly."

Style summary: 2 descriptive lines found (1 is borderline overview).

**Content:** ✅ No significant duplication. The dependency-injected logger pattern is intentionally different from root's `createLogger` convention — appropriate to document.

**Staleness:** ✅ All 31 references verified.

---

### packages/discord/CLAUDE.md

**Style:**
- Line 28: "It supports three levels (`minimal`, `standard`, `verbose`) and auto-redacts sensitive fields in verbose mode."
  → Suggested: "Supports three levels: `minimal`, `standard`, `verbose`. Auto-redacts sensitive fields in verbose mode."

Style summary: 1 descriptive line found.

**Content:**
- Lines 33–38: Development commands section (`pnpm test`, `pnpm build`, `pnpm typecheck`, `pnpm lint`) duplicates root CLAUDE.md lines 40–48. These commands are identical to root — no Discord-specific variation.
  → Fix: Remove the commands section, or document only Discord-specific commands if any exist.

**Staleness:** ✅ All 15 references verified.

---

### packages/slack/CLAUDE.md

**Style:** ✅ No descriptive lines requiring rewrite. Consistently uses imperative language.

**Content:** ✅ No significant duplication. Error types and logging are appropriately specialized to Slack context.

**Staleness:** ✅ All 14 references verified.

---

## Coverage Gaps

### Directories That Should Have a CLAUDE.md

#### `docs/`
- **Three-question test:**
  - Different tech stack: **Yes** — Astro/Starlight site, completely different from TypeScript packages. Uses `.astro` components, `.mdx` content files, `astro build`/`astro dev` instead of `tsc`/`vitest`.
  - Specialized conventions: **Yes** — Mermaid diagrams require `astro build && astro preview` (not `astro dev`), Cloudflare Pages deployment, landing page components don't use `herd-*` tokens, sidebar manually maintained in `astro.config.mjs`.
  - Repeated Claude mistakes: Requires human input.
- **Recommendation:** Create a CLAUDE.md (~25 lines) covering: Astro/Starlight stack, build/preview commands, sidebar management, Mermaid rendering caveat, Cloudflare Pages deployment, landing page component location, D2 diagram workflow reference.

### Directories Evaluated — No CLAUDE.md Needed

- `agents/` — runtime artifacts, not code requiring conventions
- `canon/` — read-only reference material
- `examples/` — standalone demos with own READMEs
- `integration-tests/` — shell-based tests, self-documented
- `scripts/` — single file, covered by `.claude/rules/git-worktrees.md`
- `packages/web/src/client/` — covered by parent `packages/web/CLAUDE.md`
- `packages/web/src/server/` — covered by parent `packages/web/CLAUDE.md`
- `packages/core/src/` subdirectories — all follow parent conventions
- `packages/cli/src/commands/` — covered by parent
- `packages/chat/src/session-manager/` — follows parent patterns
- `packages/discord/src/` subdirectories — covered by parent
- `packages/slack/src/` subdirectories — covered by parent

---

## Remediation Checklist

### Priority 1: Stale References (actively misleading)
1. [ ] `CLAUDE.md` line 61: Update coverage thresholds from "85%" to actual values or "vary per package"
2. [ ] `CLAUDE.md` line 82: Update architecture docs count from "14 pages" to "15 pages" (or remove count)

### Priority 2: Content Violations (duplicated or wrong-level content)
3. [ ] `packages/core/CLAUDE.md` line 21: Shorten logging convention to only the core-specific relative import detail
4. [ ] `packages/web/CLAUDE.md` line 48: Remove duplicated test location line (inherited from root)
5. [ ] `packages/discord/CLAUDE.md` lines 33–38: Remove duplicated development commands section

### Priority 3: Style Issues (descriptive lines → imperative rewrites)
6. [ ] `CLAUDE.md` line 7: "We are a live project" → "Follow Semver"
7. [ ] `CLAUDE.md` line 99: "Documentation lives in" → "Keep documentation in"
8. [ ] `packages/core/CLAUDE.md` line 26: "All public API surfaces are re-exported" → "Re-export all public API surfaces"
9. [ ] `packages/cli/CLAUDE.md` line 20: "This is a thin client" → "Treat this as a thin client"
10. [ ] `packages/cli/CLAUDE.md` line 26: "If you find yourself" → "Move any business logic"
11. [ ] `packages/cli/CLAUDE.md` line 41: "Tests live in" → "Place tests in"
12. [ ] `packages/web/CLAUDE.md` line 14: "Handled entirely via" → "Use CSS custom properties"
13. [ ] `packages/chat/CLAUDE.md` line 35: "components accept" → "Pass interface to components"
14. [ ] `packages/discord/CLAUDE.md` line 28: "It supports" → "Supports"

### Priority 4: Size Violations
15. [ ] `.claude/rules/diagrams.md`: Extract color palette table to a standalone reference file, reduce rules file to ~30 lines pointing to it
16. [ ] `agents/engineer/CLAUDE.md`: Add to gold standard inventory with target range, or trim

### Priority 5: Coverage Gaps (suggestions)
17. [ ] Create `docs/CLAUDE.md` (~25 lines) for Astro/Starlight conventions

### Priority 6: Inventory Updates
18. [ ] Add `agents/engineer/CLAUDE.md` to gold standard inventory table
19. [ ] Update gold standard inventory line counts if they've drifted from actuals
