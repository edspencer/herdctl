---
"@herdctl/core": minor
---

Add `tools` config field for tool availability restriction (whitelist). Unlike `allowed_tools` which only controls permission prompts, `tools` restricts which built-in tools are available to the agent entirely.